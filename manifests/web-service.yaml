apiVersion: v1
kind: Service
metadata:
  name: jobvacancy-svc
  labels:
    app: jobvacancy
spec:
  selector:
    app: jobvacancy
  type: NodePort
  ports:
    - port: 80
      name: http
      targetPort: 80
      protocol: TCP